!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    = render 'layouts/head'
  %body
    = render 'layouts/header'
    = render 'layouts/flash'
    = yield
    = render 'layouts/footer'
    = debug(params) if Rails.env.development?
    = debug(session[:players]) if Rails.env.development?
    = "session[:mg] #{session[:mg]}" if Rails.env.development?
    = "session[:rule] #{session[:rule]}" if Rails.env.development?
    = "session[:league] #{session[:league]}" if Rails.env.development?
    = include_gon
    / js
    %script{async: "async", defer: "defer", src: "https://www.line-website.com/social-plugins/js/thirdparty/loader.min.js"} / LINEシェアリンク用
    %script{crossorigin: "anonymous", integrity: "sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM", src: "https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"}
    %script{src: "https://cdn.jsdelivr.net/npm/chart.js"} // chart.js
    :cdata