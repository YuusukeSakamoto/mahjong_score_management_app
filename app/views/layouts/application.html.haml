!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    = render 'layouts/head'
  %body
    = render 'layouts/header'
    = render 'layouts/flash'
    = yield
    = render 'layouts/footer'
    = include_gon
    - if Rails.env.development?
      = debug(session[:players])
      %p.mb-0= "session[:mg] #{session[:mg]}"
      %p.mb-0= "session[:rule] #{session[:rule]}"
      %p.mb-0= "session[:league] #{session[:league]}"
      %p.mb-0= "session[:previous_url] #{session[:previous_url]}"
      %p.mb-0 ---------- カスタムエラー ----------------
      %p.mb-0= "session[:errors] #{session[:errors]}"
      %p.mb-0= "session[:rule_params] #{session[:rule_params]}"
    / js
    %script{crossorigin: "anonymous", integrity: "sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM", src: "https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"}
    %script{src: "https://cdn.jsdelivr.net/npm/chart.js"} // chart.js
    :cdata