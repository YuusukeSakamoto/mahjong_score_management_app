.col-12.text-center.pb-2
  %p.w-lg-50.pt-3.fs-3.text-purple プレイヤー
  
  .d-flex.justify-content-center
    %table.border.col-10.col-md-6
      %tr.border
        %th.border.px-2 ID
        %th.border プレイヤー
      - session[:players].each do |i|
        %tr.border
          %td.border= Player.find(i).id
          %td.border= Player.find(i).name