.container
  .row.d-flex.justify-content-center.text-main
    .col-12.text-center.pb-2
      .d-flex.justify-content-between.align-items-end.border-green-bottom-title_main.mt-4
        .d-flex.align-items-end
          %h4.text-center.text-md-start.fw-bold.mb-0 アカウント招待可能プレイヤー
  .row.text-main.fs-sm.fw-bold
    %p.mb-0 ・あなたがこれまでに対局記録をつけたプレイヤーをアカウント招待することができます
    %p ・発行される招待リンクよりアカウント登録することで、これまで記録した成績を引継ぐことができます
    
  .row.justify-content-center.text-main
    - if @invited_players.blank?
      %p.fs-bold.text-center.mt-4 招待可能プレイヤーはいません
    - else
      .col-10.col-md-6.mt-3.table-responsive.text-center
        %table.table.text-main.text-nowrap.col-10.col-md-6.fs-table
          - @invited_players.each do |player| 
            %tr
              %td.fw-bold= player.name
              %td= link_to 'アカウント招待', new_players_invitation_path(player_id: player.id), 
                  class: 'btn px-2 py-1 rounded text-white bg-orange',data: { "turbolinks" => false }
              %td= link_to '詳細', player_path(player), class: 'btn px-2 py-1 rounded text-white bg-green'